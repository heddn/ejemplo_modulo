{#
/**
 * @file
 * Default theme implementation for displaying an example progress bar.
 *
 * Available variables:
 * - percentage: percentage complete.
 */
#}
<progress title="{{ percentage }}%" value="{{ percentage }}" max="{{ maximum }}" class="super-bar">{{ percentage }}%</progress>
